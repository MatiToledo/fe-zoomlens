export enum UserRoleEnum {
  REGISTER = "register",
  REGISTER_BAR_CLOSURE = "registerBarClosure",
  REGISTER_TICKET_CLOSURE = "registerTicketClosure",
  TREASURY = "treasury",
  TREASURY_NIGHT = "treasuryNight",
  TREASURY_CENTRAL = "treasuryCentral",
}

export enum UserBORoleEnum {
  ADMIN = "admin",
  PARTNER = "partner",
  AUDITOR = "auditor",
}

export enum RetirementTypeEnum {
  TICKET = "registerTicket",
  BAR = "registerBar",
}

export enum RegisterTypeEnum {
  REVENUE = "revenue",
  EXPENSE = "expense",
}

export enum RetirementFinishTypeEnum {
  TICKET = "registerTicket",
  BAR = "registerBar",
}

export enum FixedCostTypeEnum {
  PROPERTY = "property",
  SERVICE = "service",
  TAX = "tax",
  SALARY = "salary",
  OTHER = "other",
}
